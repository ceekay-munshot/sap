{
  "name": "sap-ai-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Daily practitioner-sentiment radar for SAP's AI strategy, with recency-weighted scoring.",
  "scripts": {
    "collect": "node collector/run.mjs",
    "collect:dry": "node collector/run.mjs --dry-run",
    "serve": "node --run serve:py || python3 -m http.server 8080 --directory web",
    "check": "node collector/selftest.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "zod": "^3.25.0"
  },
  "engines": { "node": ">=20" }
}
