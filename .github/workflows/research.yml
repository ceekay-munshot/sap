name: Deep research (paid, on demand)

# The expensive pass: Claude reads the web and writes the scored reports with
# quotes. Never runs on a schedule — someone has to ask for it.
on:
  workflow_dispatch:
    inputs:
      only:
        description: 'Research a single topic id (e.g. joule_sentiment); blank runs all nine'
        required: false
        type: string
      model:
        description: 'Model override (blank picks the provider default)'
        required: false
        default: ''
        type: string

  # The dashboard's Run Research button prefers workflow_dispatch, but falls
  # back to this when its token may write contents and not Actions. Same job,
  # different doorway.
  repository_dispatch:
    types: [research]

permissions:
  contents: write

concurrency:
  group: research
  cancel-in-progress: false

jobs:
  research:
    runs-on: ubuntu-latest
    # The two doorways name their arguments differently; the job reads one pair.
    env:
      ONLY: ${{ inputs.only || github.event.client_payload.only || '' }}
      MODEL: ${{ inputs.model || github.event.client_payload.model || vars.RESEARCH_MODEL || '' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci || npm install --no-audit --no-fund

      - name: Self-test
        run: node collector/selftest.mjs

      - name: Research
        env:
          # Retrieval
          FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}
          # Reasoning — Bedrock when AWS creds are present, else the Anthropic API
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ vars.AWS_REGION || 'us-east-1' }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          AI_PROVIDER: ${{ vars.AI_PROVIDER || '' }}
          RESEARCH_MODEL: ${{ env.MODEL }}
        # Passed as a shell variable, not interpolated into the command: a
        # repository_dispatch payload comes from outside.
        run: |
          if [ -n "$ONLY" ]; then
            node collector/run.mjs --only="$ONLY"
          else
            node collector/run.mjs
          fi

      - name: Commit the reports
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # npm can touch the manifests; a dirty tree makes the rebase refuse.
          git checkout -- package.json package-lock.json 2>/dev/null || true
          git add data web/data
          if git diff --staged --quiet; then
            echo "no change"
            exit 0
          fi
          git commit -m "data: research run $(date -u +%Y-%m-%d)"
          pushed=0
          for i in 1 2 3 4; do
            if git pull --rebase origin ${{ github.ref_name }} && \
               git push origin HEAD:${{ github.ref_name }}; then
              pushed=1; break
            fi
            echo "push attempt $i failed; retrying"; sleep $((2 ** i))
          done
          if [ "$pushed" -ne 1 ]; then
            echo "::error::reports committed but could not be pushed"
            exit 1
          fi
